# Placeholder Dockerfile for hAI.OmniRoute
# This Dockerfile assumes you will copy the contents of the official
# OmniRoute repository here and use its Dockerfile for builds.

FROM debian:stable-slim

CMD ["echo", "Please replace this Dockerfile with the official OmniRoute Dockerfile or your custom build."]
